# Jinjaneering metadata config
# Entity: m_joiner_lkp
# Generated automatically from IICS mapping analysis

entity: m_joiner_lkp
source:
  database: source_db
  schema: public
  table: src_employees
target:
  schema: IDMC_Target
  model: IDMC_Target
keys:
  primary: [ORDER_ID]
  business: [ORDER_ID, CUSTOMER_ID]
fields:
  tracked: [CUSTOMER_ID, STATUS, SALESMAN_ID, ORDER_DATE, ORDER_ID]
  source: [ORDER_ID, CUSTOMER_ID, STATUS, SALESMAN_ID, ORDER_DATE, ORDER_ID, CUSTOMER_ID, STATUS, SALESMAN_ID, ORDER_DATE, auditrunddate]
load:
  type: full
  incremental_column: null
joins:
  - type: inner
    join_type: joiner
    keys: [ORDER_ID, CUSTOMER_ID, STATUS, SALESMAN_ID, ORDER_DATE]
  - type: left
    join_type: lookup
    keys: [ORDER_ID, CUSTOMER_ID]
  - type: additional_source
    name: src_users
    source_system: Source1
    fields: [ORDER_ID, CUSTOMER_ID, STATUS, SALESMAN_ID, ORDER_DATE, auditrunddate]
