version: 2

sources:
  - name: source_db
    schema: public
    tables:
      - name: src_employees
        description: "Primary source table for m_joiner_lkp"
        columns:
          - name: ORDER_ID
            tests:
              - not_null
          - name: CUSTOMER_ID
            tests:
              - not_null
          - name: STATUS
            tests:
              - not_null
          - name: SALESMAN_ID
            tests:
              - not_null
          - name: ORDER_DATE
            tests:
              - not_null
      - name: lkp_users
        description: "Lookup reference table for m_joiner_lkp"
        columns:
          - name: ORDER_ID
            tests:
              - not_null
      - name: src_users
        description: "Additional source (Source1) for m_joiner_lkp"
        columns:
          - name: ORDER_ID
            tests:
              - not_null
          - name: CUSTOMER_ID
            tests:
              - not_null
          - name: STATUS
            tests:
              - not_null
          - name: SALESMAN_ID
            tests:
              - not_null
          - name: ORDER_DATE
            tests:
              - not_null
