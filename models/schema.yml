version: 2

models:
  - name: joiner_lookup
    description: "Joiner + Lookup Enrichment - Jinjaneering generated pattern model"
    config:
      tags: ['jinjaneering', 'joiner_lookup']
    columns:
      - name: ORDER_ID
        description: "Field ORDER_ID"

      - name: CUSTOMER_ID
        description: "Field CUSTOMER_ID"

      - name: STATUS
        description: "Field STATUS"

      - name: SALESMAN_ID
        description: "Field SALESMAN_ID"

      - name: ORDER_DATE
        description: "Field ORDER_DATE"


