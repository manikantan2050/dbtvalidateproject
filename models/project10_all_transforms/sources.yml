version: 2
sources:
  - name: idmc_source_p10
    schema: public
    tables:
      - name: source_p10
        description: "Source table for m_AllTransformationMapping"
        columns:
          - name: employee_id
            tests:
              - not_null
          - name: first_name
            tests:
              - not_null
          - name: last_name
            tests:
              - not_null
          - name: email
            tests:
              - not_null
          - name: phone
            tests:
              - not_null
      - name: source_p10_mapplet
        description: "Source table for mplt_QuaterlySal"
        columns:
          - name: Salary
            tests:
              - not_null
