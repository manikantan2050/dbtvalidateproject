# Jinjaneering metadata config
# Entity: M_Mass_Ingestion
# Generated automatically from IICS mapping analysis

entity: M_Mass_Ingestion
source:
  database: IDMC_Source
  schema: public
  table: IDMC_Source
target:
  schema: IDMC_Target
  model: IDMC_Target
keys:
  primary: [employee_id]
  business: [employee_id, first_name]
fields:
  tracked: [first_name, last_name, department, job_title, region]
  source: [employee_id, first_name, last_name, department, job_title, region, base_salary, bonus_amount, hire_date, status, performance_rating, manager_id]
load:
  type: incremental
  incremental_column: LAST_UPD_TS
