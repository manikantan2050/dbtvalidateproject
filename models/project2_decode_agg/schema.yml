version: 2

models:
  - name: fact_aggregation
    description: "Fact Aggregation - Jinjaneering generated pattern model"
    config:
      tags: ['jinjaneering', 'fact_aggregation']
    columns:
      # Entity: m_decode_agg
    tests:
      - not_null
      - unique:
          column_name: "{{ pk_field }}"

