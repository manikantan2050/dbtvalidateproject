# Jinjaneering Migration — dbt Project

> Auto-generated by **Jinjaneering Studio** (Capgemini x Farmers Insurance)
> Migration factory for enterprise data modernization

## Overview

| Metric | Value |
|--------|-------|
| Patterns Discovered | 1 |
| Entities Configured | 1 |
| Source Systems | 1 |
| Reduction | 1 scripts → 1 models (0%) |

## Patterns

- **Joiner + Lookup Enrichment** (`joiner_lookup`) — 1 entities

## Project Structure

```
├── dbt_project.yml        # Project configuration
├── profiles.yml           # Connection profiles (dev/staging/prod)
├── packages.yml           # dbt package dependencies
├── macros/
│   ├── hash_change.sql           # MD5 change detection macro
│   ├── generate_surrogate_key.sql # Surrogate key generation
│   └── audit_columns.sql         # ETL audit trail columns
├── models/
│   ├── schema.yml         # Model documentation & tests
│   ├── sources.yml        # Source declarations
│   ├── patterns/          # Parameterized Jinja models
│   │   └── *.sql
│   └── entities/          # Per-entity metadata configs
│       └── *_meta.yml
└── tests/
    └── *_test.sql         # Pattern validation tests
```

## Quick Start

```bash
# 1. Install dependencies
dbt deps

# 2. Test connection
dbt debug

# 3. Run all models
dbt run

# 4. Run tests
dbt test

# 5. Generate docs
dbt docs generate && dbt docs serve
```

## Source Systems

- `Source.public.Source`

---
*Generated by Jinjaneering Studio — Capgemini Migration Factory*
