name: 'dbtvalidateproject'
version: '1.0.0'
config-version: 2
profile: 'dbtvalidateproject'
model-paths:
  - models
macro-paths:
  - macros
test-paths:
  - tests
analysis-paths:
  - analyses
seed-paths:
  - seeds
clean-targets:
  - target
  - dbt_packages
